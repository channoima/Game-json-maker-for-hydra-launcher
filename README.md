Sources json manager for hydra launcher

it use IGDB for banners preview but it is not required
but if you want to use it just go to https://dev.twitch.tv/console/apps/
1. create a private app
2. copy and paste your app_id and app_secret to the .env file

just launch the app.py and go to 127.0.0.1:5000
all games added are in the games.json

pip command:
pip install flask requests python-dotenv
